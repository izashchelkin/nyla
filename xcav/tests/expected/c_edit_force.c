// Test fixture for xcav -- C functions
#include <stdio.h>

int foo(int x) {{
    return x + 1;
}

int bar(int x) {
    return x * 2;
}

int baz(int x) {
    return x - 1;
}

int qux(int x) {
    return x / 2;
}
