cd $NYLAROOT/build/sources/bash*
make distclean || echo already clean
./configure              \
	--prefix=/usr    \
	--disable-static
make
make DESTDIR=$DESTDIR install

rm $DESTDIR/usr/share/info/dir
